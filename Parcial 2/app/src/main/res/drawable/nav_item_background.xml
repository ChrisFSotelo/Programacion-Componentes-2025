<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Al estar seleccionado -->
    <item android:drawable="@color/colorItemSelected" android:state_checked="true" />

    <!-- Al estar presionado (efecto tipo hover en táctil) -->
    <item android:drawable="@color/colorItemPressed" android:state_pressed="true" />

    <!-- Por defecto -->
    <item android:drawable="@android:color/transparent" />
</selector>
