<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Campo válido y enfocado -->
    <item android:color="@android:color/holo_green_dark" android:state_focused="true" android:state_enabled="true"/>

    <!-- Campo con error -->
    <item android:color="@android:color/holo_red_dark" android:state_enabled="true" android:state_activated="false"/>

    <!-- Estado normal -->
    <item android:color="#AAAAAA"/>
</selector>
